# wicked_pdf_chartjs
To play with pdf generation you need to:
1. clone repo
2. go to repo folder and run `bundle install`
3. run `ruby lib/pdf_generator.rb` from console to generate new pdf file in `/tmp/test.pdf`
